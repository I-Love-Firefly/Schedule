# 课表助手 2.1 更新日志

发布日期：2026 年 8 月 26 日  
版本号：2.1（versionCode 2）

## 应用商店更新说明

- 新增课程表截图智能识别：选择完整课表图片，即可通过 DeepSeek API 提取课程。
- 识别结果支持逐项校对、修改和删除，确认后才会写入本地课表。
- 使用设备端 OCR 对课程名称、星期和时间进行复核，降低错误结果直接写入的风险。
- 修复不同屏幕宽度下课程时间换行、占用空间过大的显示问题。
- 修复清空课程后桌面小组件日期和星期不再更新的问题。
- 旧版本升级用户首次启动时会看到一次 2.1 更新说明；全新安装用户仍只显示隐私政策。
- 升级 SQLite 与 OCR 组件，修复已知安全漏洞并改善新 Android 设备兼容性。

截图识别需要联网。您主动选择的图片和设备 OCR 版面信息会通过 HTTPS 发送至开发者网关，并由 DeepSeek 官方 API 完成结构化识别；API Key 不会保存在客户端。请在保存前核对识别结果。

最低系统要求调整为 Android 6.0（API 23）。

## 详细变更

### 新增

- 使用“从课程表截图导入”替代原有的特定学校教务系统网页导入流程。
- 支持识别包含完整星期栏及时间或节次信息的常见网格课程表。
- 新增识别结果预览页，可编辑课程名称、星期、地点、起止时间和教师。
- 支持删除错误识别项，并要求用户明确勾选确认后再写入数据库。
- 自动导入时保留用户手动添加的课程。
- 新增服务器端课程表识别网关，DeepSeek API Key 仅保存在服务器端。
- 新增 Android 设备端中文 OCR、网格几何分析和云端结果交叉校验。
- 新增公开课程表样本及自动化解析测试。

### 修复与优化

- 为课程卡片时间区域设置稳定宽度和单行显示，避免 `A.M.`、`P.M.` 在部分设备上换行。
- 即使课程数据为空，桌面小组件也会继续刷新当前日期和星期。
- API 识别失败、结果不完整或未通过校验时，不会自动写入课程数据。
- 对重复课程、无效星期、异常时间以及缺少 OCR 原文依据的结果进行过滤。
- SQLite 运行时改为显式初始化，提升数据库启动稳定性。
- 更新 SQLite 原生库，移除依赖扫描发现的已知高危漏洞。
- 更新 ML Kit 中文 OCR 组件，并验证关键原生库符合 Android 16 KB 页面大小要求。
- 更新隐私政策，说明截图识别的数据传输路径和第三方服务使用情况。

### 兼容性与注意事项

- 本版本仅在 Android 上提供课程表截图识别。
- 截图识别依赖网络连接，服务不可用时仍可继续使用手动添加课程功能。
- 不同学校的课表布局差异较大，识别完成后请务必逐项校对。
- 应用仍将最终确认的课程数据保存在设备本地数据库中。
- 最低支持版本由 Android 5.0（API 21）调整为 Android 6.0（API 23）。

## 对比基准

本日志根据 GitHub 默认分支 `origin/master`（2.0，versionCode 1，提交 `aa359ea`）与 2.1 发布分支（versionCode 2，提交 `fe908b8`）的代码差异整理。
